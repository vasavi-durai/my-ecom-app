import 'package:flutter/material.dart';

class CartModel extends StatelessWidget {
  const CartModel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
   body: Text("hii"),
    );
  }
}